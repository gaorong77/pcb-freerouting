[project]
name = "pcb-freerouting"
version = "0.1.0"
description = "Freerouting integration pipeline for USB Audio Board PCB"
requires-python = ">=3.10"
dependencies = [
    "shapely>=2.0",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
